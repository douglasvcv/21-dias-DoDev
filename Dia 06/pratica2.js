let conta = Number(prompt("Digite um número"))


for(let c = conta; c <= conta + 2; c++){
    
    for(let numero = 0; numero <= 10; numero++){
        console.log(`${c} x ${numero} = ${c*numero}`)
    }

}
